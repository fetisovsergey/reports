<div class="followericons">
    <% if @users.any? %>
      <ul>
        <% @users.each do |user| %>
          <li><%= link_to image_tag(user.avatar.url(:thumb),class: "img-circle miniavatar"), user %></li>
        <% end %>
      </ul>
    <% end %>
  </div>