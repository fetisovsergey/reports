<%= form_for(current_user.relationships.find_by(followed_id: @user),
             html: { method: :delete },remote: true) do |f| %>
  <%= f.submit "Вы подписаны", class: "btn btn-success",style:"font-size: 1.0em;font-weight: 300;"  %>
<% end %>
